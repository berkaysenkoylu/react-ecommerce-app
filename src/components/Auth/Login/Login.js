import React from 'react';

// import classes from './Login.module.scss';

const login = (props) => {
    return (
        <div>
            LOGIN COMPONENT
        </div>
    )
}

export default login;