import React from 'react';

import Slider from '../Slider/Slider';

const Home = (props) => {
    return (
        <div>
            <Slider />
            <div>
                The rest of the page will be here
            </div>
        </div>
    )
}

export default Home;